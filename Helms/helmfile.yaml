repositories:
  - name: bitnami
    url: https://charts.bitnami.com/bitnami

releases:
  - name: cassandra
    chart: ./charts/cassandra
    set:
      - name: dbUser.password
        value: cassandra
