apiVersion: v1
kind: Service
metadata:
  name: zookeeper-service
spec:
  type: {{ .Values.service.type }}
  ports:
  - protocol: TCP
    port: {{ .Values.service.port }}
    targetPort: 2181
  selector:
    app: zookeeper
