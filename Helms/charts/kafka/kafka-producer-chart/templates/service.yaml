apiVersion: v1
kind: Service
metadata:
  name: kafka-producer-service
spec:
  type: {{ .Values.service.type }}
  ports:
  - protocol: TCP
    port: {{ .Values.service.port }}
    targetPort: 9092
  selector:
    app: kafka
